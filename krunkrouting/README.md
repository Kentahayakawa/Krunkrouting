# Krunkrouting (front-end)

Run this docker container via `docker-compose up --build`, and then connect at `localhost:5000`. To run tests, use the sibling `flask-api` folder and follow the instructions in there.